# Demo Script

Open with:

> "Want to see what an adversary could infer from public traces around a base in under 30 seconds?"

Suggested flow:

1. Enter `Fort Liberty`.
2. Run in `Live` mode first.
3. Point out the exposure score and category breakdown.
4. Open the threat brief and explain that the narrative is built from multiple public-source hints.
5. Toggle layers to show how separate weak signals combine into a stronger adversary picture.
6. If the network or collectors are unreliable, switch to `Demo` mode or play the recorded fallback clip.
