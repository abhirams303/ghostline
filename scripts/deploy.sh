#!/usr/bin/env bash
set -euo pipefail

echo "Deploy frontend via Vercel and backend via Render or Railway."
echo "This is a placeholder script; wire provider CLIs when deployment targets are finalized."
